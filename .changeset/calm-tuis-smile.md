---
"eve": patch
---

Expose an embeddable development terminal UI from `eve/tui`, with product-owned model flows (including seeded `/model`), header tips, provider labels, and optional Vercel auth warnings. The packed TUI import is dependency-clean, while agent loading and Nitro workflow transforms preserve application paths containing `#` or `?`, including `/@fs/` IDs with query suffixes.
